spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/order_service_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:29092}

user-service:
  base-url: ${USER_SERVICE_BASE_URL:http://host.docker.internal:8080}
